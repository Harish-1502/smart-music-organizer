import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.harish.musicorganizer',
  appName: 'Smart Music Organizer',
  webDir: 'dist'
};

export default config;
